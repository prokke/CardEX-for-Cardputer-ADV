/*
 * CardEX - File Manager & Text Editor for M5Stack Cardputer ADV
 *
 * This file is intentionally almost empty.
 *
 * The Arduino IDE requires a .ino file named after the sketch folder in order to
 * recognise the project, but it also compiles every source file under src/
 * recursively. The actual entry points - setup() and loop() - live in
 * src/main.cpp so that PlatformIO (see platformio.ini) can build the exact same
 * sources without any Arduino-specific preprocessing.
 *
 * Do not add code here. Edit src/main.cpp instead.
 */
